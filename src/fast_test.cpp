#include <chrono>
#include <iostream>
#include <Eigen/Dense>
#include <Eigen/Geometry>

#include <sophus/so3.hpp>

bool test() {
  return false;
}

int main(int argc, char** argv) {
  for(int i = 0; i < 1024; i++) {
    test();
  }

  return 0;
}